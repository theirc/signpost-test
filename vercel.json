{
    "version": 2,
    "routes": [
        { "src": "/api/(.*)", "dest": "/api/$1" },
        { "src": "/[^.]+", "dest": "/", "status": 200 }
    ],
    "functions": {
        "api/*.ts": {
            "runtime": "vercel-typescript@1.0.0"
        }
    }
}