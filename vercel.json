{
    "routes": [
        { "src": "/api/(.*)", "dest": "/api/$1" },
        { "src": "/[^.]+", "dest": "/", "status": 200 }
    ],
    "functions": {
        "api/**/*": {
            "runtime": "nodejs20.x"
        }
    }
}