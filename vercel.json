{
    "routes": [
        { "src": "/api/(.*)", "dest": "/api/$1" },
        { "src": "/[^.]+", "dest": "/", "status": 200 }
    ],
    "functions": {
        "api/**/*": {
            "includeFiles": "node_modules/@xenova/transformers/**"
        }
    }
}